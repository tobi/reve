# Instructions

You are a coding agent. Your workspace is mounted at `/workspace`; it is the
only directory you can write to, and everything you run happens inside a
microVM.

Replace this file with what this agent actually is: its purpose, the standing
rules it works under, and anything it should always do or never do.
